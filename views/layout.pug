doctype html
html
  head
    title= title
    link(rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css')
    link(rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css' integrity='sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO' crossorigin='anonymous')
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    nav.navbar.navbar-expand-md.navbar-dark.bg-dark
      a.navbar-brand(href='/dashboard') Service App
      button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarCollapse', aria-controls='navbarCollapse', aria-expanded='false', aria-label='Toggle navigation')
        span.navbar-toggler-icon
      #navbarCollapse.collapse.navbar-collapse
        ul.navbar-nav.mr-auto
          if user
            li.nav-item
              a.nav-link(href='/dashboard') Dashboard
                span.sr-only (current)
            li.nav-item
              a.nav-link(href='/orders') Order
                span.sr-only (current)
            li.nav-item
              a.nav-link(href='/orders/cancle') Cancle Order
                span.sr-only (current)
          else
            li.nav-item
              a.nav-link(href='/orders') Order
                span.sr-only (current)
        ul.navbar-nav.navbar-right
          if user
            li.nav-item
              a.nav-link(href='#' style='color: #fff; font-size: 16px;') #{user.username}
            li.nav-item
              a.nav-link(href='/register') Sign Up
            li.nav-item
              a.nav-link(href='/logout') Logout
          else
            li.nav-item
              a.nav-link(href='/login') Login
    .container
      != messages('message', locals)
      if errors
        each error, i in errors
          div(class="alert alert-danger") #{error.msg}
        //================
      block content
        //================
      br
      hr
      footer
        p Copyright &copy; 2018


    script(src='https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js')
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js')

